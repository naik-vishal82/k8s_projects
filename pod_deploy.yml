kind: Deployment
apiVersion: apps/v1
metadata:
  name: deployment
spec:
  replicas: 5            
  selector:
    matchLabels:
      name: vishal                             
  template:                
    metadata:
      name: deploypod
      labels:            
        name: vishal
    spec:
     containers:
       - name: c1
         image: ubuntu
         command: ["/bin/bash", "-c", "while true; do echo Hello-Vishal; sleep 5 ; done"]
