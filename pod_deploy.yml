kind: Deployment
apiVersion: apps/v1
metadata:
  name: deployment
  annotations:
    kubernetes.io/change-cause: "My Comment"
spec:
  replicas: 2 
  selector:
    matchLabels:
      name: vishal                             
  template:                
    metadata:
      name: deploypod
      labels:            
        name: vishal
    spec:
     containers:
       - name: c2
         image: ubuntu
         command: ["/bin/bash", "-c", "while true; do echo Hello, Welcome; sleep 5 ; done"]
